/**
 * DEPRECATED: Mock agents for testing old system
 * 
 * This file is kept for reference but is currently disabled after migration to OpenAI Agents SDK.
 * If needed in the future, update the types to match the new SDK-based system.
 * 
 * To re-enable, update imports and types to match current API structure.
 */

// Commented out to prevent import errors after SDK migration
// Can be updated and re-enabled if mock testing is needed

export function isMockingEnabled(): boolean {
  return false; // Always disabled after SDK migration
}

/*
import type {
  QualifierRequest,
  QualifierResponse,
  AssessorRequest,
  AssessorResponse,
  AnalyzerRequest,
  AnalyzerResponse,
  ThreadResponse,
} from './api';

... (rest of file commented out)
*/
